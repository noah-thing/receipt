<!-- Thanks for the PR. Keep it focused; small changes merge fastest. -->

## What this changes

## Why

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` is green (added a test if behavior changed)
- [ ] `npm run build`, and `dist/` is committed if it changed
